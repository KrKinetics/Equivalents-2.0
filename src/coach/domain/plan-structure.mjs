/**
 * Coach plan structure constants (categories + meal slots).
 * Pure data — no I/O, DOM, or nutrition formula logic.
 */

export const CATS = ['pro', 'fec', 'leg', 'fru', 'lai', 'lip', 'whey'];

export const MEAL_COUNT = 7;
