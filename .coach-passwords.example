# Copy to .coach-passwords.local (gitignored) and fill locally.
# Never commit real emails/passwords.
# Used only by: node scripts/coach-set-user-passwords.mjs
# and optional live password login tests.

KR_EMAIL=
KR_PASSWORD=
ELEVATE_EMAIL=
ELEVATE_PASSWORD=
